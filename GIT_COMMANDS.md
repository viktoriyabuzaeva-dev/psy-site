# 🚀 Git — Быстрые команды для деплоя

## ✅ Статус: Проект готов!

Сборка выполнена успешно, все файлы готовы к деплою.

---

## 📋 Пошаговая инструкция

### 1️⃣ Инициализация Git (если еще не сделано)

```bash
git init
```

### 2️⃣ Добавить все файлы

```bash
git add .
```

### 3️⃣ Создать первый коммит

```bash
git commit -m "Ready for Netlify: Psychology site with Next.js 14"
```

### 4️⃣ Создать репозиторий на GitHub

Зайдите на https://github.com/new и создайте новый репозиторий (например, `psychology-site`)

⚠️ **Важно:** НЕ добавляйте README, .gitignore или LICENSE — они уже есть в проекте!

### 5️⃣ Подключить удаленный репозиторий

Замените `YOUR_USERNAME` и `YOUR_REPO` на ваши данные:

```bash
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

---

## 🌐 Деплой на Netlify

После того как код на GitHub:

1. **Зайдите на [netlify.com](https://netlify.com)**
2. **Нажмите "Add new site" → "Import an existing project"**
3. **Выберите GitHub** и авторизуйтесь
4. **Найдите ваш репозиторий** в списке
5. **Настройки сборки** (заполняются автоматически из `netlify.toml`):
   - Build command: `npm run build`
   - Publish directory: `out`
6. **Нажмите "Deploy site"**

### После первого деплоя:

1. **Скопируйте URL** вашего сайта (например: `https://amazing-site-123.netlify.app`)
2. **Добавьте переменную окружения:**
   - Зайдите в Site settings → Environment variables
   - Добавьте: `NEXT_PUBLIC_SITE_URL` = `https://amazing-site-123.netlify.app`
3. **Пересоберите сайт:** Deploys → Trigger deploy → Deploy site

---

## 🔄 Обновление сайта (после первого деплоя)

Каждый раз когда вы вносите изменения:

```bash
# 1. Посмотреть изменения
git status

# 2. Добавить все изменения
git add .

# 3. Создать коммит с описанием
git commit -m "Описание изменений"

# 4. Отправить на GitHub
git push
```

Netlify автоматически обнаружит изменения и пересоберет сайт!

---

## 📝 Полезные Git команды

```bash
# Проверить статус
git status

# Посмотреть последние коммиты
git log --oneline

# Посмотреть изменения
git diff

# Отменить изменения в файле (до git add)
git checkout -- имя-файла

# Создать новую ветку
git checkout -b новая-ветка

# Переключиться на другую ветку
git checkout main

# Объединить ветку с main
git merge название-ветки
```

---

## 🆘 Решение проблем

### Ошибка: "remote origin already exists"

```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
```

### Ошибка: "failed to push some refs"

```bash
git pull origin main --rebase
git push origin main
```

### Нужно удалить последний коммит (локально)

```bash
git reset HEAD~1
```

### Нужно изменить последний коммит

```bash
# Внесите изменения в файлы, затем:
git add .
git commit --amend -m "Новое описание коммита"
```

---

## 📚 Дополнительные материалы

- **[BUILD_SUCCESS.md](./BUILD_SUCCESS.md)** — подтверждение успешной сборки
- **[DEPLOY.md](./DEPLOY.md)** — детальная инструкция по деплою
- **[QUICKSTART.md](./QUICKSTART.md)** — быстрая шпаргалка
- **[CHECKLIST.md](./CHECKLIST.md)** — чеклист проверки

---

## ✨ Все готово!

Следуйте инструкциям выше, и через 5 минут ваш сайт будет онлайн! 🎉

**Команды для копирования:**

```bash
git init
git add .
git commit -m "Ready for Netlify: Psychology site with Next.js 14"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

Не забудьте заменить `YOUR_USERNAME` и `YOUR_REPO`!
