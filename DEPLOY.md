# 🚀 Инструкция по деплою на Netlify

## Подготовка проекта

Проект уже полностью настроен для деплоя на Netlify! Все необходимые файлы конфигурации на месте.

## Вариант 1: Деплой через Git (рекомендуется)

### Шаг 1: Загрузка на GitHub

Если ваш проект еще не на GitHub:

```bash
# Инициализация Git репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Psychology site ready for Netlify"

# Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

### Шаг 2: Подключение к Netlify

1. Зайдите на [netlify.com](https://netlify.com) и войдите в аккаунт
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **"Deploy with GitHub"** (или GitLab/Bitbucket)
4. Авторизуйте Netlify для доступа к вашим репозиториям
5. Выберите репозиторий с проектом

### Шаг 3: Настройка сборки

Netlify автоматически определит настройки из `netlify.toml`, но проверьте:

- **Build command:** `npm run build`
- **Publish directory:** `out`
- **Node version:** 18 или выше (можно задать в переменных окружения)

### Шаг 4: Переменные окружения

В настройках сайта (Site configuration → Environment variables) добавьте:

```
NEXT_PUBLIC_SITE_URL=https://ваш-сайт.netlify.app
```

💡 **Совет:** Сначала получите URL от Netlify, затем добавьте переменную и пересоберите сайт.

### Шаг 5: Деплой

Нажмите **"Deploy site"** — Netlify автоматически:
- Установит зависимости (`npm install`)
- Выполнит сборку (`npm run build`)
- Опубликует статические файлы из папки `out`

## Вариант 2: Деплой через Netlify CLI

### Установка Netlify CLI

```bash
npm install -g netlify-cli
```

### Авторизация

```bash
netlify login
```

### Инициализация и деплой

```bash
# Инициализация проекта
netlify init

# Сборка проекта
npm run build

# Деплой в продакшн
netlify deploy --prod
```

## Вариант 3: Drag & Drop (быстрый тест)

1. Выполните сборку локально:
   ```bash
   npm install
   npm run build
   ```

2. Зайдите на [app.netlify.com/drop](https://app.netlify.com/drop)

3. Перетащите папку `out` на страницу

⚠️ **Внимание:** Этот метод не подходит для продакшена, так как не настроены переменные окружения и автоматические обновления.

## Проверка перед деплоем

Убедитесь, что проект собирается локально:

```bash
# Установка зависимостей
npm install

# Проверка сборки
npm run build

# Проверка линтера
npm run lint
```

Если сборка прошла успешно, проект готов к деплою на Netlify!

## После деплоя

### 1. Настройка домена (опционально)

В настройках Netlify можно:
- Использовать бесплатный поддомен `*.netlify.app`
- Подключить свой домен

### 2. Настройка автоматических деплоев

При использовании Git, каждый push в `main` будет автоматически:
- Запускать новую сборку
- Деплоить обновления

### 3. Проверка функциональности

После деплоя проверьте:
- ✅ Все страницы открываются
- ✅ Навигация работает
- ✅ Форма записи через Telegram функционирует
- ✅ SEO мета-теги на месте (через View Source)
- ✅ Sitemap доступен: `https://ваш-сайт.netlify.app/sitemap.xml`
- ✅ Robots.txt доступен: `https://ваш-сайт.netlify.app/robots.txt`

## Оптимизации (уже настроены)

✅ **Кеширование** — статические ресурсы кешируются на 1 год  
✅ **Заголовки безопасности** — защита от XSS и других атак  
✅ **Next.js плагин** — оптимальная работа Next.js на Netlify  
✅ **SEO** — sitemap, robots.txt, meta-теги  

## Полезные команды Netlify CLI

```bash
# Посмотреть статус сайта
netlify status

# Открыть dashboard сайта
netlify open

# Посмотреть логи деплоя
netlify logs

# Посмотреть список сайтов
netlify sites:list

# Локальная эмуляция Netlify
netlify dev
```

## Решение проблем

### Ошибка сборки

Проверьте версию Node.js в настройках Netlify:
```
NODE_VERSION=18
```

### 404 при навигации

Убедитесь, что в `netlify.toml` есть редирект:
```toml
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### Медленная загрузка

Проверьте оптимизацию изображений в `next.config.js`:
```js
images: {
  unoptimized: true,
}
```

## Дополнительная информация

- [Документация Netlify](https://docs.netlify.com/)
- [Next.js on Netlify](https://docs.netlify.com/frameworks/next-js/overview/)
- [Netlify плагин для Next.js](https://github.com/netlify/next-runtime)

---

**Готово к деплою! 🎉**
