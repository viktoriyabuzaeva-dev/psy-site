# 🚀 Быстрая шпаргалка - Деплой на Netlify

## Команды для подготовки

```bash
# 1. Установка зависимостей
npm install

# 2. Проверка сборки (линтер + сборка)
npm run deploy:check

# 3. Локальная разработка
npm run dev
```

## Git команды

```bash
# Инициализация (если еще не сделано)
git init

# Добавить все файлы
git add .

# Создать коммит
git commit -m "Ready for Netlify deployment"

# Подключить GitHub репозиторий
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_REPO.git

# Отправить на GitHub
git branch -M main
git push -u origin main
```

## Netlify через веб-интерфейс

1. **Зайти на:** https://netlify.com
2. **Нажать:** "Add new site" → "Import an existing project"
3. **Выбрать:** GitHub → Ваш репозиторий
4. **Проверить настройки:**
   - Build command: `npm run build`
   - Publish directory: `out`
5. **Добавить переменную окружения:**
   - `NEXT_PUBLIC_SITE_URL` = `https://ваш-сайт.netlify.app`
6. **Нажать:** "Deploy site"

## Netlify через CLI

```bash
# Установить Netlify CLI (один раз)
npm install -g netlify-cli

# Войти в аккаунт
netlify login

# Инициализация проекта
netlify init

# Деплой в продакшн
netlify deploy --prod
```

## Переменные окружения для Netlify

```
NEXT_PUBLIC_SITE_URL=https://ваш-сайт.netlify.app
```

⚠️ **Важно:** Получите URL после первого деплоя, затем добавьте эту переменную и пересоберите сайт.

## Проверка после деплоя

```bash
# Открыть сайт
netlify open:site

# Посмотреть логи
netlify logs

# Статус деплоя
netlify status
```

## Структура проекта (для справки)

```
psychology-site-netlify/
├── src/
│   ├── app/              # Next.js App Router
│   └── components/       # React компоненты
├── public/               # Статические файлы
├── .env.example          # Пример переменных окружения
├── netlify.toml          # ✅ Конфигурация Netlify
├── next.config.js        # ✅ Next.js конфиг
├── package.json          # Зависимости и скрипты
└── out/                  # Сборка (генерируется автоматически)
```

## Быстрые ссылки

- 📖 [Полная инструкция](./DEPLOY.md)
- ✅ [Чеклист](./CHECKLIST.md)
- 📚 [Netlify Docs](https://docs.netlify.com/)
- 🎯 [Next.js on Netlify](https://docs.netlify.com/frameworks/next-js/overview/)

## Решение проблем

| Проблема | Решение |
|----------|---------|
| Ошибка сборки | Проверьте `npm run build` локально |
| 404 на страницах | Проверьте редиректы в `netlify.toml` |
| Медленная загрузка | Оптимизируйте изображения |
| Переменные не работают | Добавьте в Netlify и пересоберите |

---

**Всё готово! Время деплоить! 🎉**
